name: Docker

on: [push]

jobs:
  install:
    runs-on: ubuntu-latest
    if: true
    steps:

      - name: List local images
        run: docker images --all

      - name: Build Docker
        run: docker Build -t os-checker .

      - name: Run Docker
        run: docker run os-checker
